{
  "name": "nuxt-app",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "drizzle-generate": "drizzle-kit generate:pg --config=drizzle.config.ts",
    "drizzle-generate:custom": "drizzle-kit generate:pg --custom --config=drizzle.config.ts",
    "drizzle-migrate": "node drizzle.migrate.mjs"
  },
  "devDependencies": {
    "@nuxt/devtools": "latest",
    "nuxt": "^3.7.4",
    "vue": "^3.3.4",
    "vue-router": "^4.2.5"
  },
  "dependencies": {
    "@element-plus/nuxt": "^1.0.6",
    "drizzle-kit": "^0.19.13",
    "drizzle-orm": "^0.28.6",
    "element-plus": "^2.4.0",
    "postgres": "^3.4.0"
  }
}